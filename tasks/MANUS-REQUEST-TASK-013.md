# MANUS-REQUEST: TASK-013 ダッシュボード画面の再構築

**Requested By**: Manus  
**Timestamp**: 2025-01-01  
**Priority**: High  
**Track**: mobile-app

---

## 指示

TASK-013（ダッシュボード画面の再構築）を実装してください。

## 前提条件

- ✅ TASK-010: 共有データモデルの定義 - 完了
- ✅ TASK-012: モバイルアプリ状態管理の刷新 - 完了

## 参照ドキュメント

以下のドキュメントを必ず読んでから実装を開始してください：

1. **タスク仕様書**: `tasks/TASK-013-EPIC-04-DASHBOARD-REBUILD.md`
2. **ワイヤーフレーム**: `docs/implementation/wireframes/01_dashboard_main.png`
3. **モックアップ**: `docs/design/components/dashboard_mockup.png`
4. **スタイルガイド**: `docs/design/STYLE_GUIDE.md`
5. **コンポーネント設計書**: `docs/design/COMPONENT_DESIGN.md`
6. **テーマファイル**: `src/mobile/theme.ts`

## 実装手順

### 1. ブランチ作成

```bash
git checkout develop
git pull origin develop
git checkout -b feature/mobile-app/task-013-dashboard-rebuild
```

### 2. 新規コンポーネントの作成（プレースホルダー）

以下のコンポーネントをプレースホルダーとして作成してください。
完全な実装はTASK-014で行います。

```
src/mobile/components/dashboard/
├── Header.tsx              # 接続ステータス、最終更新時刻
├── ProgressSummaryCard.tsx # 全体進捗の円形プログレス
├── TrackCard.tsx           # トラック別進捗カード
└── BlockerAlert.tsx        # ブロッカーアラートバナー
```

### 3. DashboardScreen.tsx のリファクタリング

- 既存の `ProjectCard` と `LogEntry` の埋め込みを削除
- 新しいコンポーネント構造に置き換え
- `useDashboardStore` からデータを取得

### 4. スタイリング

- `theme.ts` からカラー、フォント、スペーシングをインポート
- ダークテーマ（`colors.background.primary: #111827`）を適用
- 日本語フォント（Zen Old Mincho, Noto Sans JP）を使用

### 5. 検証

- アプリを起動してダッシュボード画面が表示されること
- ワイヤーフレーム通りのレイアウトであること
- `projectStatus` が null の場合にローディング表示が出ること
- `blockers` が空の場合に `BlockerAlert` が表示されないこと

### 6. progress.md の更新

タスク完了後、`progress.md` を以下のように更新してください：

```markdown
| TASK-013 | mobile-app | ダッシュボード画面の再構築 | ✅ Done | TASK-012 |
```

また、"Completed Task Details" セクションに完了エントリを追加してください。

### 7. PR 作成

```bash
git add .
git commit -m "feat: Rebuild dashboard screen with new component structure (TASK-013)"
git push origin feature/mobile-app/task-013-dashboard-rebuild
gh pr create --base main --title "feat: Rebuild dashboard screen (TASK-013)" --body "..."
```

## 完了条件チェックリスト

- [ ] `DashboardScreen.tsx` がリファクタリングされている
- [ ] 新しいコンポーネント（Header, ProgressSummaryCard, TrackCard, BlockerAlert）が作成されている
- [ ] ワイヤーフレーム通りのレイアウトである
- [ ] `theme.ts` のスタイルが適用されている
- [ ] TypeScriptコンパイルエラーがない
- [ ] アプリが正常に起動する
- [ ] progress.md が更新されている
- [ ] PRが作成されている

---

🤖 Generated by Manus
